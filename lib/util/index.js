const ArrayMultiMap = require('./arrayMultiMap')
const MultiMap = require('./multimap')
const { getIdls, parseIdlFile } = require('./readParseIdl')

module.exports = {
  ArrayMultiMap,
  MultiMap,
  getIdls,
  parseIdlFile
}
